// LIC v2 — /settings/general (Phase 2.B étape 6/7, contenu rempli étape 7)

export default function SettingsGeneralPage() {
  return (
    <div className="text-muted-foreground text-sm">
      {/* Contenu réel — réglages applicatifs (DM SELECT-PX, app_name…) — étape 7. */}
    </div>
  );
}
