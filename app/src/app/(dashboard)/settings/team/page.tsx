// LIC v2 — /settings/team (Phase 2.B étape 6/7, contenu rempli étape 7)

export default function SettingsTeamPage() {
  return (
    <div className="text-muted-foreground text-sm">
      {/* Contenu réel — table team_members (CRUD SADMIN) — étape 7. */}
    </div>
  );
}
