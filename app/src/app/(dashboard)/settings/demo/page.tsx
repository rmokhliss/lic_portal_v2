// LIC v2 — /settings/demo (stub PhaseStub stylé DS).

import { PhaseStub } from "@/components/shared/PhaseStub";

export default function SettingsDemoPage() {
  return (
    <PhaseStub
      phase={null}
      label="Outils démo"
      description="Réinitialisation et régénération des données de démonstration (ADR 0007)."
    />
  );
}
